{
  "name": "@jacquesparis/objects-model",
  "version": "1.0.11",
  "description": "model class share between client and server",
  "main": "lib/index.js",
  "types": "lib/index.d.ts",
  "scripts": {
    "build": "npm run format && npm run lint && tsc",
    "format": "prettier --write \"src/**/*.ts\" \"src/**/*.js\"",
    "lint": "tslint -p tsconfig.json",
    "lib": "npm run build && git add -A && git commit --amend --no-edit && git push -f origin HEAD && npm publish --access=public"
  },
  "author": "JacquesParis",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/JacquesParis/objects-model/issues"
  },
  "homepage": "https://github.com/JacquesParis/objects-model#readme",
  "devDependencies": {
    "prettier": "^1.14.3",
    "tslint": "^5.11.0",
    "tslint-config-prettier": "^1.15.0",
    "typescript": "^3.1.5"
  }
}
